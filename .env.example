# Django Settings
DEBUG=True
SECRET_KEY=replace-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
POSTGRES_DB=automate
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Celery / Redis
CELERY_BROKER_URL=redis://redis:6379/0
REDIS_URL=redis://redis:6379/1

# OpenAI (for LLM and Audio providers)
OPENAI_API_KEY=sk-...

# SecretRef Keys
# These correspond to keys you will configure in SecretRef providers
# e.g., if you use EnvSecretsResolver, mapping refs to env vars.
# SECRET_OPENAI_KEY=...
