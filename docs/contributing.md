# Contributing

## Development Setup

1. Clone the repo.
2. Install dependencies: `pip install -e ".[dev]"`
3. Run tests: `pytest`

## Release Process

1. Build wheels: `hatch build`
2. Publish to PyPI.
